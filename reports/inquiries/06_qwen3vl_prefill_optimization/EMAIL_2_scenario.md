# [06-2] 후속 — qbcompiler 1.3.0 문서 요청 + 구체적 시나리오 전달

| 항목 | 내용 |
| --- | --- |
| 수신 | Mobilint 황지욱 님 (참조: 서현석 매니저님) |
| 관련 | 06 회신([`REPLY.md`](REPLY.md))에 대한 후속 |
| 목적 | ① qbcompiler 1.3.0 문서 요청 ② 요청하신 구체적 시나리오 전달 ③ 미답변 2건 재확인 |
| 상태 | **발송 대기** |

---

안녕하세요~ PIA-SPACE 정서익입니다.

상세한 답변 감사합니다. dynamic RoPE 지원 계획과 vision token 제한이 임의값이라는 점,
코어모드 권장까지 큰 도움이 되었습니다.

## 1. qbcompiler 1.3.0 문서 요청

`dl.mobilint.com` 에서 **qbcompiler 1.3.0 릴리즈는 확인**했습니다.
다만 **문서는 1.2 까지만 제공되고 있어** 1.3.0 사용법을 확인하기 어려운 상황입니다.

- **1.3.0 문서(또는 릴리즈 노트)를 제공받을 수 있을까요?**
- **1.3.0 이 앞서 말씀하신 dynamic RoPE 지원 버전이 맞는지** 확인 부탁드립니다.
  맞다면 **VLM 컴파일 튜토리얼도 1.3.0 기준으로 갱신**되는지, 그 일정도 함께 알려주시면 좋겠습니다.
  (현재 SDK Tutorial 의 `compilation/vlm/` 은 `master`·`release/v1.3.0` 양쪽 모두
  이미지 사이즈 224×224 가 전제입니다)

## 2. 구체적 시나리오

말씀하신 대로 저희 시나리오를 구체적으로 정리해 드립니다.

### 2-1. 서비스 형태

| 항목 | 내용 |
| --- | --- |
| 용도 | **CCTV 영상 기반 이상상황 분류** |
| 입력 | CCTV 스트림에서 추출한 **정지 이미지 1장** (비디오 아님) |
| 원본 해상도 | **720p (1280×720)** 기준, 일부 1080p |
| 채널 수 | **20채널** — AX 미니트랙 전체는 62채널이나, **이 모델은 62채널용이 아닌 20채널용**으로 봐주시면 됩니다 |
| 요청 주기 | 채널당 **1 fps** — 매 초 20개 요청이 **동시에 도착** |
| 출력 | **분류 결과 1 토큰** (생성 없음, `max_new_tokens=1`) |
| 프롬프트 | 모든 요청에 **동일한 영문 프롬프트 1개** (아래 2-2) — **22 토큰** |
| 모델 | Qwen3-VL-**2B**-Instruct (더 큰 모델도 검토 가능) |

### 2-2. 재현용 프롬프트와 토큰 구성 (실측)

벤더 측에서 바로 재현하실 수 있도록 **저희가 쓰는 형태를 단순화한 예시**를 드립니다.
이미지는 아무거나 쓰셔도 됩니다.

```
Is this image a dog? Answer with only 'yes' or 'no'. Do not output anything else.
```

- 출력은 **`max_new_tokens=1`** 로 고정 — **1 토큰을 넘기지 않습니다.**
- `yes` / `no` 단일 토큰만 확인하며, 그 이후 decode 는 수행하지 않습니다.
- **프롬프트는 모든 요청에 동일**합니다(이미지만 채널별로 다름).

`Qwen/Qwen3-VL-2B-Instruct` 공식 프로세서로 측정한 토큰 구성입니다.

| 입력 이미지 | `image_grid_thw` | 패치 | **시각 토큰** | 텍스트 토큰 | **prefill 총 토큰** |
| --- | --- | ---: | ---: | ---: | ---: |
| 224×224 | `[1, 16, 16]` | 256 | **64** | 32 | **96** |
| 336×336 | `[1, 20, 20]` | 400 | **100** | 32 | **132** |
| 720p (1280×720) | `[1, 44, 80]` | 3520 | **880** | 32 | **912** |
| 1080p (1920×1080) | `[1, 68, 120]` | 8160 | **2040** | 32 | **2072** |

(텍스트 32 토큰 = 프롬프트 22 + chat template 부가 토큰)

**이 네 조건에서 배치 처리 시의 TTFT(prefill 시간)** 를 확인하고 싶습니다.

**운영 기준은 720p** 이고 일부 1080p 입니다.
다만 720p 로 배치 처리나 지연시간을 맞추기 어려울 경우 **336 또는 224 로 낮추는 것도 고려 중**이라
네 조건을 함께 요청드립니다.

### 2-3. 목표

| 항목 | 값 |
| --- | --- |
| **지연 목표** | 20채널 요청이 **전부 완료되기까지 1000ms 이내** |
| 핵심 지표 | **TTFT** (prefill 이후 첫 토큰까지) — 토큰 1개만 보므로 decode 단계는 무관 |
| 가용 NPU | 현재 **MLA100 2장** (**테스트 측정은 NPU 1장 기준**으로 부탁드립니다) |
| 배치 | 20장을 **배치로 묶어 처리**했을 때의 지연을 확인하고자 합니다 |

### 2-4. 해상도 요구 — 균형점을 찾고자 합니다

앞선 메일에서 말씀드린 대로, 현재 static 빌드는 **224×224 / 시각 토큰 64개**로 고정되어
CCTV 원거리 객체나 세부 단서가 사라지는 문제가 있습니다.

다만 **네이티브 해상도를 고집하는 것은 아닙니다.**
저희가 찾고자 하는 것은 **균형점**입니다 —
**해상도를 최대한 높게 유지하면서 20채널 다채널 처리가 가능한 지점**이 이상적인 목표입니다.

참고로 말씀해 주신 성능(8 batch 약 5000 TPS, NPU 1장 기준으로 이해했습니다)으로 역산하면,
NPU 2장에서 20채널을 1000ms 안에 처리하려면 **이미지당 약 500 토큰** 수준이 예산이 됩니다.
시각 토큰 500개면 대략 640×800 상당으로, 현재 224×224(64 토큰)보다는 크게 개선되지만
720p 네이티브(880 토큰)에는 못 미치는 지점입니다.

그래서 **토큰 수(해상도)별 TTFT 곡선**을 주시면 저희가 속도와 정확도의 균형점을
직접 고를 수 있어 가장 도움이 되겠습니다. 위 2-2 의 네 조건(64 / 100 / 880 / 2040 토큰)이
그 용도입니다.

### 2-5. 현재 측정값 (참고)

이전 메일에서 공유드린 값입니다. **224×224 / 시각 토큰 64개 조건**입니다.

| 채널 | MLA100 1장 | MLA100 2장 |
| ---: | ---: | ---: |
| 20 | 3026 ms | **1580 ms** |

- 코어모드 vision/text 모두 `global8`, 배치 미사용, 카드당 스레드 2개
- 카드 확장 효율 1.92×, 채널 수에 선형

말씀해 주신 코어모드 권장(**이미 batch 처리 중인 경우 vision 은 `single` 코어 8개 비동기**)은
저희 조건에 해당하는 것으로 이해했고, **저희 쪽에서도 재측정해 보겠습니다.**

## 3. 요청드리는 것

최적화된 모델과 예시 코드를 준비해 주신다니 감사드립니다.
**아래 두 가지를 주시면 저희 쪽에서 바로 진행할 수 있습니다.**

- **컴파일 절차** — 위 해상도 조건으로 MXQ 를 만드는 방법(스크립트·설정).
  저희가 해상도를 바꿔가며 균형점을 직접 탐색해야 하므로 이 부분이 특히 중요합니다.
  (`LlmConfig.Attributes` 의 `max_sequence_length` / `max_cache_length` 가 말씀하신
  토큰 길이 설정으로 이해했습니다)
- **추론 예시 코드** — 20채널 배치 + `max_new_tokens=1` 조건에서 TTFT 를 측정하는 형태

## 4. 배치 대응 빌드 — 컴파일로 가능한지 확인 부탁드립니다

지난 메일에서 여쭌 **2B 용 배치 대응 빌드**(`mobilint/Qwen3-VL-8B-Instruct-Batch16` 에
해당하는 2B 버전) 관련입니다.

**위 컴파일 절차로 배치 대응 빌드까지 만들 수 있는지**, 아니면
**별도로 제공받아야 하는지** 확인 부탁드립니다.
저희가 확인한 qbcompiler 1.2.0 에는 배치 관련 설정이 노출되어 있지 않아
1.3.0 에서 어떻게 되는지 궁금합니다.
**절차만으로 가능하다면 2B 빌드를 따로 요청드릴 필요는 없을 것 같습니다.**

---

시나리오 중 부족한 부분이 있으면 말씀해 주시면 추가로 정리해 드리겠습니다.

감사합니다.
정서익 드림
