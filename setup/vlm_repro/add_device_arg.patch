--- a/scripts/run_table.py
+++ b/scripts/run_table.py
@@ 인자 추가
     p.add_argument('--threads', type=int, default=4)
+    p.add_argument('--device', type=int, default=0,
+                   help='NPU device index (/dev/ariesN). 원본은 0 고정이었다.')
@@ Classifier 에 전달
             c = Classifier(model, decoder_path=decoder, vision_path=vision,
                            vision_mode=case['vision_mode'], text_mode=case['text_mode'],
                            threads=args.threads, max_patches=case['max_patches'],
+                           device=args.device,
                            min_pixels=min(case['requested_size'][0]*case['requested_size'][1],65536))
