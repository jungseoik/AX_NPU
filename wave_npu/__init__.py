"""PIA_Wave(zero-shot 영상 이벤트 탐지)를 ARIES NPU(pe_npu)로 돌리기 위한 패키지.

핵심 설계: **임베딩을 한 번만 뽑아 캐시**한다.
비디오 → 프레임 → PE-Core 임베딩(NPU)까지가 유일한 무거운 단계이고,
프롬프트/결정규칙/임계값 실험은 전부 캐시된 임베딩 위의 CPU numpy 연산이라
초 단위로 반복할 수 있다.
"""
